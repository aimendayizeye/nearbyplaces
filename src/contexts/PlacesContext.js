import React, { createContext } from 'react';
const placeContext = createContext()
function PlacesContext(props) {
const [places,setPlaces] = useState([])
    return (
        <div>
            
        </div>
    );
}

export default PlacesContext;